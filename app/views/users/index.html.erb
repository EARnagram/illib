<% provide(:title, "Illib.Home") %>
<div class="container">
  <h2>Illib.All</h2>
  <table class="table">
    <thead>
      <tr>
        <th>User</th>
        <th>Favorite Publisher</th>
        <th>Number of Comics</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.username, user %></td>
          <td><%= user.fav_pub %></td>
          <td><%= user.comics.length %> comics</td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
